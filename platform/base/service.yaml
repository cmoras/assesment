apiVersion: v1
kind: Service
metadata:
  name: data-pipeline
spec:
  type: ClusterIP
  selector:
    app: data-pipeline
  ports:
  - name: http
    port: 5000
    targetPort: http
    protocol: TCP
  - name: metrics
    port: 8000
    targetPort: metrics
    protocol: TCP
