apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: data-pipeline

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - configmap.yaml

commonLabels:
  app: data-pipeline
  managed-by: kustomize
